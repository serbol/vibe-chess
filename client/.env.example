# Where the client should connect over WebSockets. In local dev, leave at localhost.
# In production set to the Render server URL, e.g. https://vibe-chess-server.onrender.com.
VITE_SERVER_URL=http://localhost:3000
